extends layout
block variables
  include variables
block content
    br
    .container
      h1.mt-5 Список кураторов
      br
      ul(class="list-group")
      each val in res
        li(class="list-group-item serviceList" id = val.id ) #{val.lastname} #{val.firstname} #{val.secondname}
          i(class="caret fa fa-caret-down")
        div(class="slide")
          br
          a(class="btn btn-dark" href='/curator/' + val.id role="button") Изменить
          font()=" "
          a(class="btn btn-danger" href='/delCurator/' + val.id role="button") Удалить
          p
